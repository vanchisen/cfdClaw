# 2026-03-27

- Spinner_S10: backed up `spinner.udf` to `spinner.udf.bak-20260327-2233` and added collision handling.
  - Added wall bounds (global mesh min/max) + restitution/friction params; computed in `UDF_Setup()` via MPI_Allreduce.
  - Implemented `updateCollsionForce()` to handle wall collisions (axis-aligned bounds) and particle–particle collisions (overlap correction + impulse).
  - Refactored `updateParticlePosition()` to: update positions → apply collisions once → update relative positions/angles.
  - Removed old hard clamp at z=1..22 (now uses mesh bounds).
  - On 2026-03-28, fixed follow-up compile bugs in `~/scratch/zwang/nekRS/Spinner/spinner.udf` on CCV after the collision refactor:
    - unified `updateParticleCollsionForce()` call to `updateCollsionForce()`
    - fixed `usrData`/`usrdata` mismatches, including `.data()` copy
    - added missing forward declaration `void updateCollsionForce();`
  - Added startup protection for the wall-bounce model so it is not used at the very beginning of the run:
    - introduced `collisionWarmupSteps`
    - gated wall bouncing with `spmCounter >= collisionWarmupSteps`
    - increased warm-up from 50 to 200 steps
  - After compile issues were fixed, the run advanced to a numerical failure (`pgmres: invalid residual norm`), so the collision startup gate was added as the first stabilization step.
